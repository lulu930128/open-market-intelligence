"use client";

/**
 * Deprecated component.
 *
 * The active watchlist management UI has been moved to
 * SidebarWatchlistExplorer.tsx. This file is intentionally kept as a safe
 * placeholder so older imports will not break the build while the project is
 * being refactored.
 */
export default function WatchlistManager() {
  return null;
}
